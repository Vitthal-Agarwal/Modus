# modus (backend)

Python backend for the Modus VC Audit Tool. See the top-level project README for full documentation.

## Quick start

```bash
uv sync
uv run pytest
uv run modus --help
uv run uvicorn modus.api:app --reload
```
